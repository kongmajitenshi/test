week02
- linux command : ls, ls -al, pwd, cd .git, cd .., echo "abcd" >> README.md, cat README.md
- git command 
	git init 
	git status
	git add week01
	git config --global user.email "you@example.com"
	git config --global user.name "your name"
	git commit -m "v0"
	git commit -m "v1"
	git log
	git checkout.hashcode
	git checkout -
	git branch -M main
	git remote add origin https://github.com/kongmajitenshi/test.git
	git remote -v
	git push -u origin main
	git clone https://github.com/kongmajitenshi/test.git